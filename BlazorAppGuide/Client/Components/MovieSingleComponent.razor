@using Model.Entitties
<div style="margin-right: 2%; margin-bottom: 10px; text-align: center;">
    <a href="@movieURL">
        <img src="@Movie.Poster" alt="Poster" style="width: 225px; height: 300px;" />
    </a>
    <p style="max-width: 225px; height: 44px; font-size: 15px; font-weight: bold;"><a href="@movieURL">@Movie.TitleFit</a></p>
    
    <div>
        <a class="btn btn-info">Edit</a>
        <button class="btn btn-danger" @onclick="(() => DeleteMovie.InvokeAsync(Movie))">Delete</button>
    </div>
</div>

@code {
    [Parameter]
    public Movie Movie { get; set; }
    [Parameter]
    public EventCallback<Movie> DeleteMovie { get; set; }

    private string movieURL = string.Empty;

    protected override void OnInitialized()
    {
        movieURL = $"movie/";
    }
}
